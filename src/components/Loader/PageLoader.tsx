import Loader from './Loader';

const PageLoader = () => {
  return (
    <div className="w-full h-[calc(100vh_-_95px)] flex items-center justify-center">
      <Loader />
    </div>
  );
};

export default PageLoader;
